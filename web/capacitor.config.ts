import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omniverso.oficialhelper',
  appName: 'OficialHelper',
  webDir: 'dist'
};

export default config;
